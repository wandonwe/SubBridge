# SPA fallback — every route serves the single-page app.
# The frontend calls the API by its absolute Worker URL (baked in at build
# time via VITE_API_BASE), so there is no /api proxy here: Cloudflare Pages
# cannot proxy /api/* to an external Worker anyway.
/*  /index.html  200
