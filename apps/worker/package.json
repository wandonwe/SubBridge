{
  "name": "@subbridge/worker",
  "version": "1.0.0",
  "private": true,
  "description": "SubBridge edge API — Hono on Cloudflare Workers",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run --outdir dist",
    "deploy": "wrangler deploy",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@subbridge/converter": "workspace:*",
    "@subbridge/core": "workspace:*",
    "@subbridge/parser": "workspace:*",
    "@subbridge/utils": "workspace:*",
    "hono": "^4.12.2",
    "uqr": "^0.1.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260620.0",
    "typescript": "^5.9.3",
    "wrangler": "^4.63.0"
  }
}
